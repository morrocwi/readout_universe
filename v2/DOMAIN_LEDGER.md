# Domain Ledger — one row per bridge, artifact-gated (v2.0-dev)

> Rule: a domain enters this ledger ONLY with a runnable/checked artifact at its
> canonical path. Tier legend as in `README.md`; verdict classes as in
> `research_universal_solver/docs/root/BORROWED_VS_DERIVED_LEDGER.md`.
> Paths are relative to `<workspace-root>/` (private repos; git remotes
> in each repo). A bridge row is `Dr` by definition — the artifact's own tier is
> listed separately.

| # | Domain | Bridge claim (this book ↔ artifact) | Artifact (canonical) | Artifact tier | Falsifier of the bridge |
|---|--------|-------------------------------------|----------------------|---------------|-------------------------|
| D1 | Logic (home) | the finite knower's logic = residual flow at τ_c | `readout_universe/code/` LTP1–4 + `UPL_Sorites.v` | `finite_diagnostic` + `Th_coqc` | any LTP claim failing its check command |
| D2 | Physics — one spine | logic's grammar A ↔ L_R of the spine PDE (B0) | `research_universal_solver/docs/root/EQUATION_OF_EVERYTHING.md` + `formal/` | mixed; per-element in ledger | LTP phenomenon unposable on L_R (or converse) |
| D3 | Physics — universal step | knower = S3 regime (τ_c > τ_c\*) of the one step | `.../docs/root/UNIVERSAL_STEP_ALL_THINGS.md` (+ cited `Th_coqc` files) | `Dr` over `Th_coqc` | an agency-like system violating the τ_c > τ_c\* regime split |
| D4 | Paradox/infinity | Ω_∞ diagnosis (book §L-10) = infinity-injection diagnosis (corpus) | `.../docs/root/INFINITY_INJECTION_DIAGNOSIS.md` | `Dr` (P1–P3 stated) | a paradox surviving with NO injected infinity |
| D5 | Mathematics | irrational = non-readout capstone; discrete cores close in ℚ | `.../docs/root/KNOWLEDGE_MAP.md` capstone + `formal/*.v` (axiom-free) | `Th_coqc` per file | a "cannot close in ℚ" wall NOT of readout-gap form |
| D6 | Biology/health | body–mind dynamics as spine readouts (cusp/relax/homeostasis/epidemic/EP-3) | `research_universal_solver/formal/Info{HealthCuspFold,HealthCausalRelax,BioHomeostasis,EpidemicThreshold,CoupledCuspEP3}_attempt.v` (5 files, present; existence re-verified 2026-07-19) + `.../docs/engineering/SPINE_SYSTEM.md` + `.../docs/root/READOUT_HEALTH_SYNTHESIS.md` | `Th_coqc` (structure; per-file profile at source); EP-3 clinical = `Dr`/Open | a health equation of the set with no spine-PDE form |
| D7 | Mind/AI — bounded knower | LTP3 divergence-iff-load-bearing ↔ measured 0.5B multi-hop ceiling + honest abstention | `research_pgcross.universalModel` refutation chain (REFUTATION_CHAIN, N=40 pilot) | `finite_diagnostic` (pilot; confirm ≥300 pending) | a bounded model recovering load-bearing discarded info w/o escalation |
| D8 | Ethics | L-16: shared correction structurally forced among finite knowers | `main.tex` ch. Ethics (L-16) — derivation-sketch only | `Dr`; first-order content declared non-derivable | a stable multi-knower system with closed correction channels |
| D9 | Aesthetics / politics | compression-perception / correction-infrastructure readings | `main.tex` (Open Frontier ch.) | `Open` (no LTP exists) | — (not yet a claim; listed to mark the boundary) |

## Coverage honesty

- **Bound nodes (three-file binding met): 1** — sorites (D1).
- D2–D7 have real artifacts but the *bridges* are `Dr` — declared, falsifiable, unproven.
- D8–D9 are inside v1.0 already; no elevation.
- Domains NOT listed (chemistry, economics, …): **not covered**; adding them
  requires an artifact, per the rule at top.
