# Retention Self-Interaction Unit — AP20

## Question

Paper IV measured the geometric spin ladder

\[
\kappa_{\rm geo}(0,\tfrac12,1)=\{0,1,1\}.
\]

Thus plain Hodge/1-form geometry supplies one vector-curvature unit, while the Nielsen–Hughes target requires two. The open question was whether the missing unit must be borrowed as a Yang–Mills trilinear vertex, or whether it follows from the retention grammar’s own ordered channel composition.

## Conditional forcing statement

Assume:

1. **One ordered composition.** Channels compose by a bilinear associative product \(m(x,y)\).
2. **Circulation as order defect.**
   \[
   K(x,y)=m(x,y)-m(y,x).
   \]
   This is the exchange-odd readout of the same ordered product, not a separately inserted bracket.
3. **Self-carrier closure.** A channel fluctuation belongs to the same compositional algebra as the background channel. It is not merely an external payload.
4. **Quadratic load.** The readout uses the product-induced quadratic norm \(\tfrac12\lVert K\rVert^2\).

Then bilinearity forces

\[
\begin{aligned}
K(A_x+\varepsilon a_x,A_y+\varepsilon a_y)
={}&K(A_x,A_y)\\
&+\varepsilon\bigl(K(A_x,a_y)+K(a_x,A_y)\bigr)\\
&+\varepsilon^2K(a_x,a_y).
\end{aligned}
\]

The self-composition term \(K(a_x,a_y)\) appears with coefficient exactly one. There is no independent coefficient available to tune.

Expanding the quadratic load gives the forced cross term

\[
\langle K_0,K(a_x,a_y)\rangle,
\]

whose Hessian contributes one curvature unit. Therefore, at the declared finite operator scope,

\[
1_{\rm geometry}+1_{\rm composition}=2,
\qquad
2^2=4.
\]

## Executed artifact

`ap/ap20_retention_self_interaction.py`

The tests verify:

- the order defect is exchange-odd and satisfies Jacobi because it descends from associative composition;
- the \(\varepsilon^2\) self-term is generated by the single composition law with unit coefficient;
- its Hessian contributes exactly one curvature unit with zero residual in the finite \(\mathfrak{so}(3)\) representation;
- a commutative channel algebra produces no unit;
- an external-payload model produces no quadratic self-term;
- combining the independently executed Paper-IV geometric unit with the AP20 compositional unit gives \(2\), hence quadratic response \(4\).

## What changed

Before AP20:

\[
1_{\rm geometry}+1_{\rm Yang\text{-}Mills\ borrow}=2.
\]

After AP20:

\[
1_{\rm geometry}+1_{\rm ordered\ self\text{-}carrier\ composition}=2.
\]

The Yang–Mills commutator is no longer used as the starting primitive in the diagnostic; it is recovered as the exchange-odd defect of one ordered product.

## Honest status

This closes the **missing-unit algebra** conditionally on assumptions 1–4. It does **not** yet prove that RD4 alone uniquely forces those assumptions. In particular, the following remain open:

- why the minimal physical channel algebra must be noncommutative;
- why self-carrier closure rather than external-payload transport is selected;
- why the physical representation is the required three-generator algebra;
- ghost, longitudinal and heat-kernel bookkeeping toward the complete \(+11/3\) beta coefficient;
- generation of the Standard Model gauge group and representation table.

Accordingly, the strongest permitted statement is:

> Given one bilinear associative ordered channel product, circulation as its exchange-odd defect, self-carrier closure and quadratic load, the missing spin-1 self-interaction unit is forced and the coupling-level ladder reaches \(1+1=2\Rightarrow4\) without inserting a separate trilinear vertex.

The remaining foundational falsifier is now one level lower: derive assumptions 1–4—especially noncommutativity and self-carrier closure—from the retained-distinction grammar rather than declaring them.
