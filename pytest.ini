[pytest]
python_files = test_*.py ap*.py
# Intentional scope: pytest covers ap/ only. The v1.0 LTP battery in code/
# is run standalone (`make verify`), not via pytest. If you add test_*.py
# elsewhere, extend testpaths accordingly.
testpaths = ap
