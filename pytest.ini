[pytest]
python_files = test_*.py ap*.py
# Intentional scope: ap/ (case-study diagnostics) plus tests/ (omega/
# native_logic unit tests). The v1.0 LTP battery in code/ is run
# standalone (`make verify`/`scripts/ci_verify.sh`), not via pytest.
testpaths = ap tests
